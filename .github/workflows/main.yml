name: Docker Image CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  update_resume:
    runs-on: ubuntu-latest
    name: main.yml work
    steps:
      
      - name: Checkout
        uses: actions/checkout@v3
      - name: start action.yml
        uses: ./

      - run: ls
      - run: git config --global user.email "ramusdmitry@gmail.com"
      - run: git config --global user.name "Dmitry Ramus"
      - run: git add cv/cv.pdf
      - run: git status
      - run: git commit -m "cv.pdf"
      - run: git push origin HEAD:main --force
